version: 2

aliases:
   - &run_selenium_tests
     name: run_selenium_tests
     command: |
        echo "hello...from test_selenium xxx"

jobs:
   test_selenium:
      docker:
         - image: selenium/standalone-chrome:3.1.0
      steps:
         - checkout
         - run: *run_selenium_tests

workflows:
   browser_tests:
      jobs:
         - test_selenium
